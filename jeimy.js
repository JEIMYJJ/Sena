// Define los números de la tabla y el límite aquí
const numMul =7;  // Define el número de la tabla de multiplicar
const numLimit = 10;  // Define el límite de multiplicación

console.log(`Tabla de multiplicar del ${numMul} hasta el multiplicador ${numLimit}`);
for (let i = 1; i <= numLimit; i++) {
    console.log(`${numMul} x ${i} = ${numMul * i}`);
}
